using System;

namespace HolaMundo
{
    class Program
    {
        static void Main(string[] args)// Lo que esté dentro de esta frase entre llaves ({, }) es lo que define el punto de entrada de la aplicación.
        {
            Console.WriteLine("Hello World!"); //Esta línea sirve para que se imprima por el cmd la frase: Hello World!
            Console.ReadKey(); //Esta línea sirve para que el programa no continue hasta que el usuario pulse una tecla
        }
    }
}
